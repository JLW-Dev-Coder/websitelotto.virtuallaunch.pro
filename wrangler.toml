name = "websitelotto-virtuallaunch-pro"
pages_build_output_dir = ".next"

# Secrets — set via: wrangler secret put SECRET_NAME
# STRIPE_SECRET_KEY (shared with VLP Worker)
# STRIPE_WEBHOOK_SECRET (shared with VLP Worker)
