{
  "slug": "brand-mood",
  "fields": [
    {
      "id": "h2_pick_a_mood",
      "type": "text",
      "label": "H2 — Pick a Mood",
      "default": "Pick a Mood"
    },
    {
      "id": "h2_what_s_included",
      "type": "text",
      "label": "H2 — What's Included",
      "default": "What's Included"
    },
    {
      "id": "h2_preview",
      "type": "text",
      "label": "H2 — Preview",
      "default": "Preview"
    },
    {
      "id": "h2_reviews",
      "type": "text",
      "label": "H2 — Reviews",
      "default": "Reviews"
    },
    {
      "id": "h3_vote_the_best_mood",
      "type": "text",
      "label": "H3 — 🗳️ Vote the Best Mood",
      "default": "🗳️ Vote the Best Mood"
    },
    {
      "id": "h3_mood_name",
      "type": "text",
      "label": "H3 — ${mood.name}",
      "default": "${mood.name}"
    },
    {
      "id": "h3_p_title",
      "type": "text",
      "label": "H3 — ${p.title}",
      "default": "${p.title}"
    },
    {
      "id": "button_sign_in",
      "type": "text",
      "label": "Button — Sign In",
      "default": "Sign In"
    },
    {
      "id": "button_send",
      "type": "text",
      "label": "Button — Send",
      "default": "Send"
    },
    {
      "id": "button_name_pct",
      "type": "text",
      "label": "Button — ${name}${pct}%",
      "default": "${name}${pct}%"
    },
    {
      "id": "button_r_count",
      "type": "text",
      "label": "Button — ${r} ${count}",
      "default": "${r} ${count}"
    },
    {
      "id": "button_p",
      "type": "text",
      "label": "Button — ${p}",
      "default": "${p}"
    },
    {
      "id": "link_brandmood",
      "type": "text",
      "label": "Link — BrandMood",
      "default": "BrandMood"
    },
    {
      "id": "link_home",
      "type": "text",
      "label": "Link — Home",
      "default": "Home"
    },
    {
      "id": "link_pick_a_mood",
      "type": "text",
      "label": "Link — Pick a Mood",
      "default": "Pick a Mood"
    },
    {
      "id": "link_what_s_included",
      "type": "text",
      "label": "Link — What's Included",
      "default": "What's Included"
    },
    {
      "id": "link_preview",
      "type": "text",
      "label": "Link — Preview",
      "default": "Preview"
    },
    {
      "id": "link_reviews",
      "type": "text",
      "label": "Link — Reviews",
      "default": "Reviews"
    },
    {
      "id": "link_get_the_template",
      "type": "text",
      "label": "Link — Get the Template",
      "default": "Get the Template"
    },
    {
      "id": "link_get_template_license",
      "type": "text",
      "label": "Link — Get Template + License →",
      "default": "Get Template + License →"
    }
  ]
}
