{
  "slug": "wedding-upgrade",
  "fields": [
    {
      "id": "h1_make_your_weddinglook_expensive",
      "type": "text",
      "label": "H1 — Make your weddinglook expensive.",
      "default": "Make your weddinglook expensive."
    },
    {
      "id": "h2_what_s_included",
      "type": "text",
      "label": "H2 — What's Included",
      "default": "What's Included"
    },
    {
      "id": "h2_preview_the_suite",
      "type": "text",
      "label": "H2 — Preview the Suite",
      "default": "Preview the Suite"
    },
    {
      "id": "h2_4_curated_themes",
      "type": "text",
      "label": "H2 — 4 Curated Themes",
      "default": "4 Curated Themes"
    },
    {
      "id": "h2_what_brides_are_saying",
      "type": "text",
      "label": "H2 — What Brides Are Saying",
      "default": "What Brides Are Saying"
    },
    {
      "id": "h2_print_ready_in_minutes",
      "type": "text",
      "label": "H2 — Print-ready in minutes.",
      "default": "Print-ready in minutes."
    },
    {
      "id": "h3_which_theme_is_your_favorite",
      "type": "text",
      "label": "H3 — Which theme is your favorite?",
      "default": "Which theme is your favorite?"
    },
    {
      "id": "h3_item_title",
      "type": "text",
      "label": "H3 — ${item.title}",
      "default": "${item.title}"
    },
    {
      "id": "button_post_comment",
      "type": "text",
      "label": "Button — Post Comment",
      "default": "Post Comment"
    },
    {
      "id": "button_t_colors_map_c_join_t_label_t_de",
      "type": "text",
      "label": "Button — ${t.colors.map(c => ``).join('')} ${t.la",
      "default": "${t.colors.map(c => ``).join('')} ${t.label} ${t.desc}"
    },
    {
      "id": "button_t_label_0_votes",
      "type": "text",
      "label": "Button — ${t.label} 0 votes",
      "default": "${t.label} 0 votes"
    },
    {
      "id": "link_vowvenue",
      "type": "text",
      "label": "Link — VowVenue",
      "default": "VowVenue"
    },
    {
      "id": "link_home",
      "type": "text",
      "label": "Link — Home",
      "default": "Home"
    },
    {
      "id": "link_what_s_included",
      "type": "text",
      "label": "Link — What's Included",
      "default": "What's Included"
    },
    {
      "id": "link_preview",
      "type": "text",
      "label": "Link — Preview",
      "default": "Preview"
    },
    {
      "id": "link_themes",
      "type": "text",
      "label": "Link — Themes",
      "default": "Themes"
    },
    {
      "id": "link_reviews",
      "type": "text",
      "label": "Link — Reviews",
      "default": "Reviews"
    },
    {
      "id": "link_download",
      "type": "text",
      "label": "Link — Download",
      "default": "Download"
    },
    {
      "id": "link_get_the_bundle",
      "type": "text",
      "label": "Link — Get the Bundle",
      "default": "Get the Bundle"
    },
    {
      "id": "link_get_the_bundle_29",
      "type": "text",
      "label": "Link — Get the Bundle — $29",
      "default": "Get the Bundle — $29"
    }
  ]
}
