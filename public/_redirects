# SCALE asset pages: dynamic prospect slugs are loaded at runtime from R2.
# All /asset/* paths rewrite to the single shell page Next.js exports at
# out/asset/__shell__/index.html (requires trailingSlash:true in next.config).
# The client reads the real slug from window.location.pathname.
/asset/* /asset/__shell__/ 200
