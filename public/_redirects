# SCALE asset pages: dynamic prospect slugs from R2 — rewrite all
# /asset/* requests to the single shell page built at /asset/__shell__/.
# The client reads the real slug from window.location.pathname.
/asset/__shell__/* /asset/__shell__/index.html 200
/asset/* /asset/__shell__/index.html 200
